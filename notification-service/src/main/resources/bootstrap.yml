spring:
  application:
    name: notification-service
  profiles:
    active: docker
  cloud:
    config:
      uri: http://config-server:8888
      username: admin
      password: config123
      fail-fast: false
      retry:
        initial-interval: 1000
        max-attempts: 6
        max-interval: 2000
      enabled: true